# Smart-Intrusion-Detection-System-Smart-IDS-
A hybrid Intrusion Detection System (IDS) for detecting SQL Injection attacks in web requests, combining regex rules and a local large language model (Qwen2.5 via Ollama). Includes admin dashboard and AI-assisted alert explanations.
